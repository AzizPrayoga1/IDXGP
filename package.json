{
  "name": "idxgp",
  "private": true,
  "scripts": {
    "mock-api": "node scripts/mock-api.js",
    "dev": "concurrently \"npm run mock-api\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm run build && wrangler pages deploy dist --project-name idxgp"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.114.0"
  }
}
